// import { LOGGIN_SUCCESS } from '../actions';

// let cloneOnject = function(obj) {
// 	return JSON.parse(JSON.stringify(obj))
// };

// let newState = { user: { leggedId: false } };

// export default function (state, action) {
// 	switch (action.type) {
// 		case LOGGIN_SUCCESS:
// 			newState = cloneOnject(state)
// 			newState.user.leggedId = true;

// 			return newState;

// 		default:
// 			return state || newState;
// 	}
// }
